# Bots Platform

`apps/bots/` is the durable home for Discord bot runtimes inside `latent-space-hub`.

- Active production bot today: `apps/bots/slop/`
- Deploy target for Slop: Railway, with the service root set to `apps/bots/slop`
- Future bots can be added beside Slop without splitting into another repo
